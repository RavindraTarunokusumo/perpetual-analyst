"""Fetcher protocol — all ingestion modules implement this interface."""

# TODO (Task 5/7): Implement
# Protocol:
#   class Fetcher(Protocol):
#       def fetch(self, source: Source, db: Connection) -> list[Item]:
#           """Fetch new items since source.last_fetched_at. Write to DB. Return inserted items."""
#           ...
